# Contract source

`customer.yaml` was copied from `SkyPorch/daykeeper-openapi` tag `v1.0.0`,
commit `35f5bd45fe0c6a6901766543bff90dae6838b965`.

Provider-neutral documentation wording follows
`SkyPorch/daykeeper-openapi#5`, commit
`fec6f9b88661fbfd04b7d7c66acce257f15ea6bd`. That editorial change does not
alter operations, schemas, scopes, or generated TypeScript types.

Generation pull requests must copy an immutable tagged contract and record the
tag and full commit SHA here.
